# 绿算治策 · 视觉设计方案

> 用途：定义最终产品的视觉风格、布局、交互流程
> 执行前确认方案，确认后再开发

---

## 一、整体风格

```
风格定位：专业 · 清晰 · 可信
配色基调：**深绿**（绿算治策）+ 青绿（绿色算力）+ 白（清爽）

适合场景：政府工作人员使用 / 比赛展示
目标用户：西部县市发改局/工信局工作人员
```

---

## 二、配色系统

```
主色：**深绿**     #0D5E33   → 标题、导航、主要按钮
辅色：中绿     #1A7A45   → 生态构建型标签、数据高亮
辅色：青绿     #00A07A   → 资源转化型标签、增长数据
警示色：橙     #E67E00   → 风险提示、你的数据
亮绿高亮     #7AE0A0   → 标题高亮文字
背景色：浅绿    #F2F8F4   → 页面底色
卡片色：白     #FFFFFF   → 内容卡片
文字主色：深灰  #1A2A1E   → 正文
文字辅色：中灰  #4A6A5A   → 次要文字
分割线：淡灰   #E0E6EE   → 分割线、边框
```

---

## 三、页面布局（响应式）

### 3.1 桌面端（≥1024px）两栏布局

```
┌──────────────────────────────────────────────────────────┐
│  🌱 绿算治策             绿色算力产业治理决策支持系统       │
│                   中国国际大学生创新大赛（2026）              │
├──────────────────────────────────────────────────────────┤
│  一句话定位：为西部地区提供绿色算力产业发展路径参考            │
├──────────────────────┬───────────────────────────────────┤
│                      │                                    │
│  📋 输入地区参数       │  📊 分析结果                        │
│                      │                                    │
│  ① 行政层级  [输入框]  │  路径判定：[生态构建型] 标签          │
│  ② 财政收入  [输入框]  │  匹配度：██░░░░ 73%               │
│  ③ 企业数量  [输入框]  │                                    │
│  ④ 用电价格  [输入框]  │  雷达图（四维模型可视化）             │
│  ⑤ 年均气温  [输入框]  │                                    │
│  ⑥ 产业用地  [输入框]  │  参数对比柱状图                      │
│  ⑦ 应用场景  [输入框]  │                                    │
│  ⑧ 高校数量  [输入框]  │  发展建议（条目列表）                │
│                      │  风险提示（条目列表）                  │
│  [📂载入参考] [分析]   │                                    │
│                      │  ▼ 参考案例数据对比（可折叠）          │
├──────────────────────┴───────────────────────────────────┤
│  数据来源：新疆统计局 · 克拉玛依市政府 · 伊吾县政府 · 信通院   │
│  绿算治策 · 绿色算力产业治理决策支持系统                      │
└──────────────────────────────────────────────────────────┘
```

### 3.2 移动端（<768px）单栏布局

```
┌──────────────────────┐
│  🌱 绿算治策           │
│  绿色算力产业治理       │
├──────────────────────┤
│  输入参数 → 分析结果    │
│     (切换式)           │
├──────────────────────┤
│  ① 行政层级  [输入框]  │
│  ② 财政收入  [输入框]  │
│  ...                  │
│  [分析]                │
├──────────────────────┤
│  📊 结果               │
│  路径标签 + 匹配度条    │
│  雷达图（小）           │
│  建议列表               │
├──────────────────────┤
│  ▼ 参考数据             │
└──────────────────────┘
```

---

## 四、核心交互流程

```
用户打开页面
    │
    ▼
┌─────────────────────────────┐
│  看到一句话定位 + 输入表单    │
│  （默认参数为空，placeholder提示）│
└──────────┬──────────────────┘
           │ 两种操作路径
     ┌─────┴─────┐
     ▼           ▼
┌─────────┐ ┌───────────────┐
│ 手动填参数 │ │ 点击"载入参考" │
│ 逐个输入  │ │ 弹出选择菜单   │
│ 8项数据  │ │ 克拉玛依/伊吾  │
└────┬────┘ └──────┬────────┘
     │             │
     └──────┬──────┘
            ▼
     ┌────────────────┐
     │ 点击"开始分析"   │
     │ → 加载动画       │
     └────────┬───────┘
              ▼
     ┌────────────────────────────────────┐
     │ 结果区更新（动画过渡 .4s）           │
     │                                     │
     │ 1. 路径标签（大号，彩色背景）         │
     │    "生态构建型 · 参考：克拉玛依"      │
     │                                     │
     │ 2. 匹配度条形图（双色对比条）          │
     │    [████████████░░░░]               │
     │    生态构建 73%      资源转化 27%     │
     │                                     │
     │ 3. 四维模型雷达图（ECharts）          │
     │    用户 vs 克拉玛依 vs 伊吾          │
     │                                     │
     │ 4. 参数对比柱状图（ECharts）          │
     │    你的值 / 克拉玛依 / 伊吾           │
     │                                     │
     │ 5. 💡 发展建议（蓝底列表）            │
     │ 6. ⚠️ 风险提示（橙底列表）            │
     │                                     │
     │ 7. ▼ 参考案例数据（可折叠表格）       │
     └────────────────────────────────────┘
              │
              ▼
     ┌────────────────┐
     │ 用户可调整参数     │
     │ → 结果实时更新    │
     │（无需重新点击分析） │
     └────────────────┘
```

---

## 五、图表规划

### 5.1 雷达图（四维模型可视化）

```
用途：展示用户在四维模型三个维度上的得分 vs 两个基准案例

类型：ECharts 雷达图（radar）
数据系列：3条（用户 / 克拉玛依 / 伊吾）
维度轴：资源转化、公共支撑、生态构建
颜色：用户=橙 #F07A00，克拉玛依=蓝 #005FC8，伊吾=绿 #00A07A

交互：鼠标悬停显示具体数值
```

### 5.2 柱状图（参数对比）

```
用途：8个参数逐一对比用户 vs 两个基准案例

类型：ECharts 分组柱状图（bar）
数据系列：3组（用户 / 克拉玛依 / 伊吾）
X轴：8个参数名称
颜色：同上

交互：点击柱体可查看具体数值
标注：每个柱体上方标具体数字
```

### 5.3 匹配度条形图

```
用途：直观展示两种路径的匹配百分比

类型：纯CSS双色进度条
左半：生态构建型（蓝渐变）
右半：资源转化型（绿渐变）
中间：百分比数字标注
```

---

## 六、关键交互细节

### 6.1 参数输入的即时反馈

```
每个输入框变化时：
  1. 右侧数值标签实时更新
  2. 表单底色轻微闪烁（.2s过渡）
  3. 输入有效性实时校验（范围外显示红色提示）

优化：不用点按钮，参数变化→结果自动重算（debounce 500ms）
```

### 6.2 "载入参考"交互

```
点击"载入参考"→ 弹出下拉菜单
  ├── 载入克拉玛依数据（自动填入并分析）
  └── 载入伊吾数据（自动填入并分析）

选中后：
  1. 所有输入框瞬间填入对应数据（动画过渡）
  2. 结果区自动更新
  3. 输入框高亮显示"已加载参考数据"
```

### 6.3 结果区动画

```
分析完成后：
  1. 路径标签：从上方滑入（.3s）
  2. 匹配度条：从左到右填充（.5s）
  3. 雷达图：ECharts自带入场动画
  4. 建议列表：逐条淡入（每条间隔.1s）
```

### 6.4 可折叠参考案例

```
结果区底部：
  "📖 查看克拉玛依 vs 伊吾完整数据对比"
  点击展开一个带格式的对比表格
  包含：行政层级/财政/企业/电价/PUE/算力等15项对比
  每项数据旁边标注来源等级[A/B/C]
```

---

## 七、数据来源标注方式

```
所有显示的数据旁标注可信度等级：

  "一般公共预算收入：122.2亿元 [A]"
  "智能算力规模：17,000P [A]"  
  "入驻企业：185家 [B]"
  "应用场景：7类 [C]"

图例：
  [A] = 官方统计数据
  [B] = 政府公告/工作报告
  [C] = 归纳推断值（基于公开资料）
```

---

## 八、手机端特殊处理

```
输入优化：
  - 数字键盘（inputmode="decimal"）
  - 输入框高度加大（48px+）方便触屏
  - 滑块替代数字输入（可选模式）

布局调整：
  - 输入区和结果区改为上下排列
  - 结果区在输入区下方
  - 图表缩小但保持可读性
  - 操作按钮加大（min 44px touch target）

性能：
  - ECharts配置responsive自适应
  - 禁用动画（移动端）
```

---

## 九、技术实现方案

```
技术栈：纯HTML + CSS + JS + ECharts
文件结构：
  index.html     → 主页面（含HTML结构 + CSS + JS）
  （所有代码在一个文件中方便修改）

外部依赖：
  ECharts CDN → https://cdn.jsdelivr.net/npm/echarts@5/dist/echarts.min.js
  （百度产品，中国可访问）

核心算法：TOPSIS（内嵌在JS中）
数据：所有数据硬编码在JS中（data.js结构）

无需：
  × 无npm/node
  × 无数据库
  × 无后端
  × 无构建步骤
  × 无框架
```

---

> 确认此方案后，开始执行开发


---

# 附：网站使用说明

# 绿算治策 · 网站使用说明

> 用途：帮助用户理解网站的使用方法、功能流程和结果解读
> 目标用户：地方政府工作人员、参赛评委、对绿色算力规划感兴趣的研究者

---

## 一、网站是什么

**绿算治策**是一个绿色算力产业治理决策辅助工具。

它的作用是：**你输入你所在地区的8项基本数据，它帮你判断最适合走哪种发展路径，并告诉你应该优先做什么。**

它基于两个真实案例的研究成果：
- **克拉玛依**（生态构建型路径）
- **伊吾**（资源转化型路径）

你不需要懂算法，也不需要看原始数据，只需要填数字，网站自动出结果。

---

## 二、使用流程

```
第一步：打开网站
      ↓
第二步：输入地区名称，然后填写8项参数
      ↓
第三步：点击「生成产业评估报告」
      ↓
第四步：查看路径判定、四维评分、维度可视化
      ↓
第五步：查看第4部分「发展建议」和风险提示
      ↓
第六步（可选）：点击「导出报告」选择 Word 或 PDF 格式下载
      ↓
第七步（可选）：展开完整对比表查看参数明细
```

**第一步：打开网站**
在浏览器打开绿算治策页面即可，无需注册或登录。

**第二步：填写8项参数**
在左侧输入区依次填写8个数字，每个输入框右侧有单位提示（亿元、家、%等）和填写说明。如果某些数据不确定，可以参考"载入参考"功能看两个案例的数据是多少。

**第三步：点击"开始分析"**
填写完所有参数后，点击蓝色"开始分析"按钮。

**第四步：查看结果**
右侧结果区会显示：
1. 判定你适合的路径类型
2. 与两种路径的匹配度百分比
3. 你的各项参数与参考案例的对比
4. 针对性发展建议
5. 风险提示

---

## 三、页面功能分区

### 3.1 顶部导航栏

```
左侧：项目名称"绿算治策"
右侧：参赛标识"中国国际大学生创新大赛（2026）"
```

### 3.2 左侧：参数输入区

包含8个数字输入框，每个输入框对应一个维度。

**每个输入框包含：**
- 参数名称（如"行政层级指数"）
- 当前输入值（实时显示）
- 单位（如"亿元"、"家"、"元/度"）
- 快捷参考提示（最小值、最大值）

**关于"载入参考"按钮：**
- "载入参考：克拉玛依" → 一键填入克拉玛依的真实数据
- "载入参考：伊吾" → 一键填入伊吾的真实数据
- 这两个按钮的作用是**给用户参考**，看看真实的城市数据是多少
- 点击后会自动填入数据并自动分析

### 3.3 右侧：结果展示区

默认显示引导文案，点击"开始分析"后显示分析结果。

**结果包含：**
1. **路径标签**：生态构建型 / 资源转化型 / 混合型
2. **路径说明**：用一句话解释这条路径是什么
3. **相似度进度条**：两条彩条，显示你和克拉玛依的相似度、和伊吾的相似度
4. **参数对比表**：你的8项参数 vs 两个参考案例
5. **发展建议**：针对你匹配的路径给出的具体行动建议
6. **风险提示**：走这条路需要注意什么
7. **参考案例折叠区**：点开可查看克拉玛依和伊吾的详细数据对比

---

## 四、结果解读指南

### 4.1 看懂路径判定

```
结果示例：生态构建型（以克拉玛依为参考）

是什么意思：
  你的地区条件与克拉玛依更接近
  适合走"政府主导+全链条生态培育"的路子
  建议重点做：产业基金、场景开放、政策工具箱、人才引进
```

### 4.2 看懂相似度进度条

```
示例：
  🌳 生态构建型 73%  │██████████████████│  ⚡ 资源转化型 27%

怎么理解：
  73% → 你的数据和克拉玛依有73%的相似度
  27% → 你的数据和伊吾有27%的相似度
  结论：你更适合走生态构建型路径
```

### 4.3 看懂参数对比

```
参数对比表（示例）：
  ┌─────────────────────┬──────┬──────────┬─────────┐
  │      维度          │ 你的值 │ 克拉玛依 │  伊吾   │
  ├─────────────────────┼──────┼──────────┼─────────┤
  │ 年可支配财政(亿元)  │  80  │   120    │   10    │
  └─────────────────────┴──────┴──────────┴─────────┘

怎么理解：
  你的财政收入80亿，低于克拉玛依的120亿，但远高于伊吾的10亿
  说明：你虽然不如克拉玛依有钱，但比伊吾好得多 → 可能走介于两者之间的路线
```

### 4.4 看懂建议和风险

```
发展建议（示例）：
  ✅ 设立产业引导基金，构建多层次资金支持体系
  ✅ 发放算力券/运营补贴，降低中小企业用算门槛

风险提示（示例）：
  ⚠️ 防范财政补贴依赖，设定退出机制和补贴递减路径
  ⚠️ 防止产业地产化，避免为招商而招商导致空置

怎么理解：
  建议 → 你应该优先做的具体事情
  风险 → 做这些事情的时候要注意什么坑
```

---

## 五、常见问题

### Q1：有些数据我不知道怎么填怎么办？

A：可以先用"载入参考"功能看克拉玛依和伊吾的数据，大致判断你所在地区处于什么水平。比如你的地区是县级、财政较弱，可以参照伊吾的数据填。

### Q2：数据填错了影响大吗？

A：这个模型是基于趋势判断，不是精确计算。即使个别数据有偏差，整体方向不会变。建议尽量填真实数据，结果更准确。

### Q3：我的判读结果是"混合型"是什么意思？

A：说明你的条件介于两种路径之间，没有特别明显的倾向。可以看看自己哪个维度的数据更有优势，先从这个维度突破。

### Q4：这个工具能直接给出规划方案吗？

A：不能。这个工具提供的是**方向性建议**，具体的产业规划需要结合当地实际情况进一步研究。

### Q5：数据来源可靠吗？

A：所有数据都标注了可信度等级：
- [A] 官方统计数据
- [B] 政府公告/工作报告
- [C] 行业报告/企业公告
- [D] 媒体报道/估算值

可以在结果下方展开"参考案例"查看完整数据来源。

---

## 六、技术说明（供参考）

- 本工具为纯前端静态网页，无需后端服务
- 所有计算在用户浏览器中完成，数据不上传
- 算法：加权欧氏距离相似度匹配
- 输入限制：仅接受数字，会自动校验范围
- 兼容性：支持主流浏览器（Chrome/Edge/Firefox/Safari）

---

> 版本：Demo v0.1
> 用途：辅助地方政府初步判断绿色算力产业发展路径
> 注意：本工具为学术研究项目，结果仅供参考，不作为决策唯一依据
