# 绿算治策 · 部署上线方案

---

## 一、总体架构

```
你本地代码 ──push──→ GitHub仓库 ──auto──→ Vercel托管 ──→ 在线访问
                        ↑                          ↓
                    [代码备份/版本管理]     https://lvsuangov.vercel.app
                                                 (免费，国内可访问)
```

---

## 二、免费套餐说明

### Vercel（推荐，国内可打开）
- 费用：免费（Hobby 计划）
- 中国大陆：可正常打开（偶尔稍慢）
- 自动 HTTPS（SSL 证书）
- 自动从 GitHub 同步，push 即部署
- 免费域名：`xxx.vercel.app`

---

## 三、操作步骤

### 第 1 步：创建 GitHub 账号

如果你已有 GitHub 账号，跳过此步。

1. 浏览器打开 https://github.com
2. 右上角点击 **Sign up**
3. 输入邮箱 → 设置密码 → 用户名（建议 `lvsuangov` 或你的团队名）
4. 验证邮箱后即完成

---

### 第 2 步：在 GitHub 上创建仓库

1. 登录 GitHub，点击右上角 `+` → **New repository**
2. Repository name 填：`lvsuangov`
3. Description 选填：`绿色算力产业治理决策支持系统`
4. 设为 **Public**（Vercel 免费版要求公开仓库）
5. **不要勾选**"Add a README"等任何初始化选项
6. 点击 **Create repository**

创建成功后，你会看到一个页面，里面有命令行操作指引。

---

### 第 3 步：将本地代码推送到 GitHub

**在 Windows 上操作（推荐用 Git Bash）**：

1. 打开项目文件夹：
```bash
cd D:/Users/mz/学习/2026上/中观/绿算治策
```

2. 关联远程仓库（用你的 GitHub 用户名替换 `你的用户名`）：
```bash
git remote add origin https://github.com/你的用户名/lvsuangov.git
```

3. 推送代码：
```bash
git push -u origin main
```

> ⚠️ 推送时会弹出 GitHub 登录窗口（首次），按提示登录授权即可。

**验证**：刷新 GitHub 仓库页面，应该能看到所有文件已上传。

---

### 第 4 步：在 Vercel 部署

1. 浏览器打开 https://vercel.com
2. 点击 **Sign Up**（建议用 GitHub 账号直接登录）
3. 登录后点击 **Add New...** → **Project**
4. 在 "Import Git Repository" 列表中找到 `lvsuangov` 仓库，点击 **Import**
   - 如果没看到，点 "Adjust GitHub App Permissions" 授权
5. 配置页面（基本不用改）：
   - **Framework Preset**：选 **Other**
   - **Root Directory**：保持 `/`
   - **Build Command**：留空（纯静态）
   - **Output Directory**：留空
6. 点击 **Deploy**

部署过程约 30 秒，完成后你会看到 **Congratulations!** 页面和访问链接。

**访问地址**：`https://lvsuangov.vercel.app`（Vercel 自动分配）

---

### 第 5 步：关于免费域名

**目前国内可用的免费域名方案：**

| 方案 | 域名示例 | 特点 |
|------|----------|------|
| Vercel 自带 | `lvsuangov.vercel.app` | 免费，即开即用，国内可访问 |
| Cloudflare Pages | `lvsuangov.pages.dev` | 免费，国内速度较好 |
| Gitee Pages | `用户名.gitee.io/lvsuangov` | 国内最快但流程繁琐 |

**推荐直接用 Vercel 域名，无需额外申请。** 免费顶级域名（.tk/.ml 等）目前已无法稳定获取，不建议浪费时间。

> 如果你未来需要自定义域名（如 `lvsuangov.cn`），需要购买域名（约 30-50 元/年），并在 Vercel 项目设置 → Domains 中绑定。

---

### 第 6 步：后续更新流程

以后每次修改代码，只需要执行：

```bash
cd D:/Users/mz/学习/2026上/中观/绿算治策
git add index.html
git commit -m "修改了什么内容"
git push
```

Vercel 会自动检测到 GitHub 推送，**自动重新部署**，约 30 秒后更新生效。

---

## 四、替代方案（如果 Vercel 国内访问不佳）

### 方案 B：Gitee Pages（国内托管）
1. 注册 https://gitee.com
2. 创建仓库，推送代码
3. 仓库 → 服务 → Gitee Pages → 启动
4. 访问地址：`用户名.gitee.io/lvsuangov`
5. 国内访问速度最快，但配置步骤多

### 方案 C：云存储静态网站（免费额度）
- **阿里云 OSS**：需实名 + ICP 备案（流程较长）
- **七牛云**：有免费额度 + 测试域名
- **腾讯云 COS**：需备案

**结论：优先用 Vercel，如果打不开再试 Gitee Pages。**

---

## 五、注意事项

1. **首次访问**：如果 Vercel 页面显示 "This page is taking too long..."，刷新一次即可
2. **更新后不生效**：Vercel 有时有 CDN 缓存，等 1-2 分钟再访问
3. **微信内打开**：Vercel 域名在微信内可能被拦截，建议用浏览器打开或绑定自定义域名
4. **数据安全**：此工具纯前端，所有计算在你浏览器本地完成，不上传服务器

---

## 六、快捷操作清单

| # | 操作 | 耗时 |
|---|------|------|
| 1 | 注册 GitHub | 5 分钟 |
| 2 | 创建仓库 | 1 分钟 |
| 3 | 推送代码 | 2 分钟 |
| 4 | 注册 Vercel | 3 分钟 |
| 5 | 部署上线 | 1 分钟 |
| 6 | 后续每次更新 | 30 秒 |
| **总计** | **首次部署** | **约 15 分钟** |

---

> 方案版本：v1.0 · 2026-05-17
> 如有任何步骤卡住，可以随时问或让我直接操作。
